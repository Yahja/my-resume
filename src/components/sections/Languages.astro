---
import Section from "../Section.astro"

import { languages } from "@cv";

---

<Section title="Idiomas">
  <article>
    {languages.map(({language, fluency, flag}) => {
      const langFlag = (flag == "spain") ? "🇪🇸"  : (flag == "italy") ? "🇮🇹" : "🇺🇸";

      return(
        <div class="languages">
          <h3>{language}</h3>
          <span>{langFlag} {fluency}</span>
        </div>
      )
    })}
  </article>
</Section>

<style>
  article {
    display: flex;
    flex-direction: row;
    gap: 0.8rem;
    justify-content: flex-start;
  }
  .languages {
    padding: 1rem;
    border: 1px solid #f1f1f1;
    border-radius: 4px;
  }
  h3 {
    margin-bottom: 8px;
  }
  span {
    background-color: #f2f2f2;
    color: #444;
    font-size: 0.8rem;
    padding: 4px 8px;
    border-radius: 2px;
  }

  html.dark .languages {
    border: 1px solid #5234bf;
  }

  html.dark span {
    color: #f2f2f2;
    background: #5234bf;
  }
  @media print {
    html.dark .languages {
      border: 1px solid #f1f1f1;
    }
    html.dark span {
      color: #444;
      background-color: #f2f2f2;
    }
  }
</style>